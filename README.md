# RandomForest-Classifier
This project performs sentiment analysis on customer reviews using Natural Language Processing. It preprocesses text by removing special characters, converting to lowercase, eliminating stopwords, applying stemming. The cleaned reviews are transformed for analysis, enabling accurate sentiment classification and text mining using Python libraries.
